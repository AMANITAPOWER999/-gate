# Goldantelopegate v2.0 - Cryptocurrency Trading Bot

Автоматический торговый бот для Gate.io с поддержкой стратегии Parabolic SAR (5m/30m/1m alignment), x10 leverage, LONG/SHORT позициями, Flask dashboard и Telegram уведомлениями.

## Особенности

- **🤖 Автоматическая торговля**: SAR 5m/30m/1m alignment strategy
- **📊 Web Dashboard**: Real-time мониторинг позиций и графики
- **💬 Telegram**: Уведомления о сделках (@goldantelopegate_bot)
- **⚙️ Гибкая конфигурация**: Динамическая стратегия через UI
- **🔄 Paper Trading**: Режим с виртуальным балансом $100
- **💰 Реальная торговля**: Реальный Gate.io баланс при подключении API

## Развертывание на Railway

### 1. Клонировать репозиторий
```bash
git clone https://github.com/supermashaandbear/Goldantelopegate_v2.0.git
cd Goldantelopegate_v2.0
```

### 2. Установить зависимости локально (опционально)
```bash
pip install -r requirements.txt
```

### 3. Подключить к Railway

Вариант A: Через Railway CLI
```bash
railway link
railway up
```

Вариант B: Через GitHub (рекомендуется)
- Создать проект в Railway
- Подключить GitHub репозиторий
- Добавить переменные окружения

### 4. Переменные окружения (Environment Variables)

**Обязательные:**
- `GATE_API_KEY` - API ключ Gate.io
- `GATE_API_SECRET` - API секрет Gate.io
- `TELEGRAM_BOT_TOKEN` - Telegram Bot Token (от @BotFather)
- `TELEGRAM_CHAT_ID` - Ваш Telegram Chat ID

**Опциональные:**
- `RUN_IN_PAPER` = `0` (1 для paper trading, 0 для live)
- `SESSION_SECRET` - Секрет для Flask sessions (автогенерируется)
- `VIEW_PASSWORD` - Пароль для dashboard (по умолчанию `admin`)
- `USE_SIMULATOR` = `0` (1 для тестирования на симуляторе)

## Структура проекта

```
├── app.py                    # Flask веб-приложение
├── trading_bot.py           # Основная логика торговли
├── telegram_notifications.py # Telegram интеграция
├── signal_sender.py         # WebSocket сигналы
├── market_simulator.py      # Симулятор рынка для тестов
├── Dockerfile               # Docker конфигурация
├── Procfile                 # Heroku/Railway конфигурация
├── railway.json             # Railway конфигурация
├── requirements.txt         # Python зависимости
├── templates/               # HTML шаблоны
│   ├── dashboard.html      # Основной dashboard
│   ├── webapp.html         # Telegram WebApp
│   └── login.html          # Логин страница
└── static/                 # CSS, JS, изображения
```

## Стратегия торговли

### Условия входа (OPEN)
- Все выбранные таймфреймы (по умолчанию 1m, 5m, 30m) должны быть в одном направлении
- 8-второе подтверждение перед открытием позиции
- TOP-1 gainer пара из 584 контрактов Gate.io

### Условия выхода (CLOSE)
- Когда close_level (5m) меняет направление
- Игнорируются смены других таймфреймов

### Параметры
- **Leverage**: x10
- **Position Size**: 100% доступного баланса × x10
- **Symbol**: Динамический TOP-1 gainer (обновляется каждые 60 сек)
- **Timeframe Check**: Каждые 5 секунд

## Использование

### 1. Доступ к Dashboard
```
https://your-railway-url.com/
```

### 2. Кнопки управления
- **Active/Inactive** - Включить/выключить бота
- **Open Long/Short** - Ручное открытие позиции
- **Close Position** - Закрыть текущую позицию
- **Connect API** - Переключиться на реальный баланс Gate.io

### 3. Мониторинг
- Real-time график цены с SAR маркерами
- История сделок и P&L
- Баланс и текущий TOP-1 gainer

## Telegram WebApp

Подключить WebApp в Telegram Bot:
```
/setmenubutton
```

Выберите вашего бота и добавьте URL: `https://your-railway-url.com/webapp`

## API Endpoints

- `GET /api/status` - Статус бота, баланс, позиции
- `POST /api/start_bot` - Запустить бота
- `POST /api/stop_bot` - Остановить бота
- `GET /api/chart_data` - Данные для графика
- `GET /api/debug_sar` - Debug информация SAR
- `POST /api/close_position` - Закрыть позицию

## Разработка локально

```bash
# Установить зависимости
pip install -r requirements.txt

# Запустить локально
python app.py

# Откройте http://localhost:5000
```

## Лицензия

MIT

## Автор

@supermashaandbear
